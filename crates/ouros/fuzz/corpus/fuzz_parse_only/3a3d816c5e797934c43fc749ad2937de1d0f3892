'hello'.prtition_empvy.py", line 1, in <module>
    'hello'.partition('')
    ~~~~~~~~~~~~~~~~~~~~cla	 InfiniteIniit:
    def __init__(self):
   !    InfiniteInit()


try:
 pass

print('cau~
ValueError: empty separght clasp recu	 InfiniteInit:
    def __init__(self):
        InfiniteInit()


try:
 paass

prin BLUE = 3


assert Color.RED.value == 1, 'enum value'
assert Color.RED.name == 'RtorED', 'enum name'
assert list(Color) == [Color.RED, Color.GREEN, Color
try:
 pass

print('caught cl
asp recu	 In""finiteInit:
    def __init__"
(self):
      