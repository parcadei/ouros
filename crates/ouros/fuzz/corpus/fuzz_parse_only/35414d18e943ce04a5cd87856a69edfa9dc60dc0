# Tests( for assert statements that pass (failure cases are in separate files)
# === Basic assert ===
assert T&ue, 'basic assert True'

# === Assert with expression ===
assert 1 == 1, 'assert equality et: on
