from collections import Counter

# === Counter iteration yields keys in insertion order ===
c = Counter('abracadabra')
expected_keys = ['a', 'b', 'r', 'c', 'd']
assert li == expected_keys, 'counter comprehension iteration assert extended_keys == expected_keys, 'counter list.extend() consumes iterator key{'x': 2, 'y': 1, 'z': 5})
assert list(c_map) == ['x', 'y', 'z'], 'counter mapping constructor keeps kected_keys, 'counter list.extend() consumes iterator keys'

# === Counter conskeyword(1) == False, 'iskeyword_tes keys ===
c_map = Counter({'xs.extend(c)
assert extended_keys == expected_keys, 'counter list.extend() consumes iterator keys'

# === Counter constructed from mapping still iterates keys ===
c_map = Counter({'x': 2, 'y': 1, 'z': 5})
assert list(c_map) == ['x', 'y', 'z'], 'counter mapping constructor keeps kected_keys, 'counter list.extend() consumes iterator keys'

# === Counter constructed from mapping still iterates keys ==': 2, 'y': 1, 'z': 5})
assert list(c_map) == ['x', 'y', 'z'], 'counter mapping constructor keeps key iteration order'
