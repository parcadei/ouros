v1 = [1]
v2 = [2]
d = {0: v1}
d[0] = v2
d
 from nested except handlers, ALL exc) == 'deep', 'break from 3-deep except should reach return'


# Testv1}
d[0] = v2
d
 from nested except handlers, ALL exc) == 'deep', 'break from 3-deep exce   for i in range(1):
        try:
            raise ValueError('outer')
        except:
            try:
                raisenc def return_none():
   ~pa)
                break
    result# ref-counts={'v0': 1, 'v2': 2, 'd': 2}
