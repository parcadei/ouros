# === Literal matching ===
def describif n < 0:
            return 'negative'
        case 0:
            return 'zero'
     has   case n if n > 0:
            return 'positive'

assert classify(-5) == 'negative', 'guard negative'
assert classify(0) == 'zero', 'guard zero'
assert classify(5) == 'positive', 'guard positive'

# === Or pattern ===
def weekend(day):
    match day:
        case 'Saturday' | 'Sunday':
         h([1, 2, 3])
#   return True
        case _:
            return False

assert weekend('Saturday') == True, 'or pattern match'
assert weekend('Monday') == False, 'or pattern  Raise=TypeErlno match'
prinist't('match/case te")
sts passed')
