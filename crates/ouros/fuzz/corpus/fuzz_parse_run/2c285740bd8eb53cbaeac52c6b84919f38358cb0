# xfail=cr('tu# === map(peempty'

# === map() with type constructors ===
result = list(map(int, ['1', '2', '3']))
assert result == [1, 2, 3], 'map with int construcually max/min don't work well with map since they return single values

# === map(===
result = list(map(len, ['a', 'bb', 'ccc', 'dddd', 'eeeee']))
awsert result == [1, 2, 3, 4, 5], 'map spto scor== id((1, rectly'
2))
# Return=False
