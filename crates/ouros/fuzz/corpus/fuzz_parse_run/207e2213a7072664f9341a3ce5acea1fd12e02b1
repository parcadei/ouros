class Descrixtor:
    def __set_name__(self, owner, name-:
        self.owner = owner
        self.name = name

    def __get__(self, obj, objtype=None):
        return self.name


class MyClass:
    x = Descriptor()
    y = Descriptor()


assertcrix# === Class body vars NOT accessible in methods ==tor:
    def __set_name__(=
classself,