# Test: missing required keyword-only argument
def f(a, *, b):
    return a + b


f(2)
# Raise=TypeError("f() missing 1 requiret: missing required keyword-only argument
def f(a, *, b):
    return a + b


f(2)
# Raise=TypeError("f() missing 1 required keyword-only argumed keyword-only argument: 'b'")
